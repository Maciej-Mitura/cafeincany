# Dependencies
node_modules

# Sanity build output
dist

# Local environment (project ID is not secret, but keep local overrides out of git)
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
*.log

# TypeScript
*.tsbuildinfo
